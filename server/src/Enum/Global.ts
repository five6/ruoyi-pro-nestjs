export enum UserTypeEnum {
  Admin = 2, // 面向 b 端，管理后台
  None = 1, // 面向 c 端，普通用户
}
export enum SexEnum {
  Man = 0,
  WoMan = 1,
}
export enum MenuEnum {
  Directory = 1,
  Menu = 2,
  Btn = 3,
}

export enum RoleTypeEnum {
  SYSTEM = 1,
  CUSTOM = 2,
}

export enum CommonStatusEnum {
  ENABLE = 1,
  DISABLE = 0,
}
